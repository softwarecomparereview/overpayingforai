Enhance the EXISTING codebase to add production-ready SEO + AI-discovery crawl/indexing basics.

Do NOT redesign the app. Do NOT remove existing routes or SEO logic. Extend cleanly and conservatively.

Goal:
Add:

1. Dynamic `/sitemap.xml`
2. Dynamic `/robots.txt`
3. Dynamic `/llms.txt`
4. Basic indexing/discovery setup checks for both search engines and AI crawlers
5. Clear confirmation in the output of what was added and what route groups were included

Assumptions:

* Site domain is `https://overpayingforai.com`
* Existing app already has routes for pages like home, calculator, AI types, guides, comparisons, and possibly best-lists
* Existing content/data may live in JSON files under `/src/data/*`
* Existing SEO helpers may already exist. Reuse them where appropriate.
* Preserve current architecture and coding style

Tasks:

## 1) Add dynamic sitemap generation

Create a lightweight sitemap generator that includes:

* Static pages such as:

  * `/`
  * `/calculator`
  * `/ai-types`
  * any other clearly existing top-level marketing pages already in the app
* Dynamic pages derived from existing data sources where available, for example:

  * `/ai-types/:slug`
  * `/guides/:slug`
  * `/comparisons/:slug`
  * `/best/:slug`
* Only include routes that actually exist in the current project structure
* Deduplicate URLs
* Exclude admin/internal/dev-only pages such as:

  * `/admin/*`
  * test/dev/sample routes
* Output valid XML with:

  * `<loc>`
  * optional `<lastmod>` only if easy to infer safely
* Use base URL `https://overpayingforai.com`

Implementation requirements:

* Prefer a generator utility such as `/src/utils/generateSitemap.ts`
* Expose it via a real route or server handler at `/sitemap.xml`
* Return correct `Content-Type: application/xml; charset=utf-8`

## 2) Add AI-aware robots.txt

Create a dynamic or static-served `/robots.txt` that:

* Allows public crawling of normal pages
* Disallows:

  * `/admin/`
  * any obvious internal/private utility routes if present
* Includes:

  * `Sitemap: https://overpayingforai.com/sitemap.xml`
* Is intentionally AI-discovery aware:

  * Do NOT accidentally block public marketing/content pages from major AI/search crawlers
  * Unless the existing project already has a deliberate privacy policy requiring otherwise, keep public content crawlable by:

    * `Googlebot`
    * `Bingbot`
    * `OAI-SearchBot`
    * `GPTBot`
* Use conservative defaults. Do not accidentally block the whole site.

Target behavior:

* Global allow for public pages
* Explicit disallow for admin/internal paths
* No blanket disallow that harms discoverability
* If there are crawler-specific sections, they must remain consistent with the global policy and not conflict

Example acceptable structure:
User-agent: *
Allow: /
Disallow: /admin/

User-agent: OAI-SearchBot
Allow: /

User-agent: GPTBot
Allow: /

Sitemap: https://overpayingforai.com/sitemap.xml

Implementation requirements:

* Expose a real `/robots.txt` route or public file
* Return correct `Content-Type: text/plain; charset=utf-8`

## 3) Add `/llms.txt`

Add an LLM-friendly discovery file at `/llms.txt` in plain text / markdown-like format.

Purpose:
Provide a concise map of the site for LLM-based tools, assistants, and AI systems.

Requirements:

* Include site title and short description
* Include canonical site URL
* Include short guidance about what the site is for
* Include a compact list of the most important public routes and route groups, only if they actually exist:

  * homepage
  * calculator
  * AI types index
  * AI type pages
  * guides
  * comparisons
  * best lists
* Include a short section describing what content is most authoritative on the site
* Exclude admin/private/internal routes
* Keep the file concise and useful, not bloated marketing copy
* If easy to do, generate entries from the same data sources used by the sitemap so it stays in sync
* Expose it at `/llms.txt`
* Return correct `Content-Type: text/plain; charset=utf-8`

Suggested structure:

* H1 title
* one-paragraph summary
* “Key URLs”
* “Route groups”
* “Authoritative content”
* “Excluded/private areas”

## 4) Add indexing/discovery setup checks

Audit the current codebase and make the following fixes if missing:

* Ensure canonical URL support exists for major public pages
* Ensure pages are not accidentally marked `noindex`
* Ensure sitemap, robots, and llms paths are publicly reachable
* Ensure homepage and major templates have basic metadata if they already use the SEO system
* If there is an existing SEO component/helper, extend or reuse it rather than duplicating logic
* Ensure admin/internal pages remain excluded from crawl/discovery where appropriate

Do NOT invent a full SSR framework migration.
Do NOT add heavy dependencies.
Keep this lightweight.

## 5) Add simple maintainer documentation

Create a small utility comment block or lightweight README documenting:

* where sitemap URLs are assembled from
* where llms.txt entries are assembled from
* how to add future route groups safely
* what is intentionally excluded

This can be:

* short comments in the generator files, or
* `/src/utils/seo/README.md` if the structure supports it

## 6) Confirm the latest content routes are included

Very important:
After implementing, inspect the current route/data structure and explicitly confirm in your final output which of these are included in generation if they exist:

* homepage
* calculator
* AI types index
* AI type pages
* guides
* comparisons
* best-list pages

Do this for:

* sitemap
* llms.txt

If any of these do NOT currently exist in the codebase, say so clearly instead of pretending they were included.

## 7) Verification requirements

After implementation, verify:

* `/sitemap.xml` returns valid XML
* `/robots.txt` returns expected plain text
* `/llms.txt` returns expected plain text
* sitemap includes sample public URLs
* llms.txt includes the major public route groups
* admin/internal routes are excluded from sitemap and llms.txt
* robots.txt references sitemap
* robots.txt does not accidentally disallow public pages
* if crawler-specific rules were added, they do not conflict with global allow/disallow behavior

## 8) Output requirements

At the end, provide a concise implementation summary with exactly these sections:

### Files created

* list each new file

### Files updated

* list each modified file

### Public URLs added

* `/sitemap.xml`
* `/robots.txt`
* `/llms.txt`

### Route groups included in sitemap

* explicitly say yes/no for:

  * homepage
  * calculator
  * AI types index
  * AI type pages
  * guides
  * comparisons
  * best lists

### Route groups included in llms.txt

* explicitly say yes/no for:

  * homepage
  * calculator
  * AI types index
  * AI type pages
  * guides
  * comparisons
  * best lists

### Excluded from sitemap and llms.txt

* list admin/internal patterns excluded

### Verification performed

Include checks for:

* sitemap returns valid XML
* robots.txt returns expected text
* llms.txt returns expected text
* sitemap includes sample public URLs
* llms.txt includes sample key URLs
* admin routes are excluded
* sitemap URL is referenced in robots.txt
* public content is not accidentally blocked for AI/search discovery

### Sample outputs

Show:

1. exact `/robots.txt`
2. first few entries of `/sitemap.xml`
3. exact `/llms.txt`

Constraints:

* Do not break existing routing
* Do not touch pricing/calculator business logic
* Do not modify unrelated UI
* Keep code production-safe and minimal
* Prefer additive changes only
* If the app has both frontend and backend/server files, wire this into the existing serving pattern instead of inventing a second server

Final rule:
Do not guess which route groups exist. Inspect the actual repo and report the truth.
